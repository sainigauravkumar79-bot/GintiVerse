# GintiVerse

A universal tools & calculators platform: registry-driven, glass/3D visual
language, local-first smart search, no signup required.

## Getting started

```bash
npm install
npm run dev       # http://localhost:5173
npm run build     # runs the sitemap generator, then builds to dist/
